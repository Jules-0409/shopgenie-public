# ShopGenie — 进度台账

> 最后更新：2026-06-17
> 当前阶段：V1.5 与运营闭环首版可用；商品图工作室前端已下线

---

## 当前状态

V1.5 已具备三平台生成、商品事实库、内容版本、质量闭环、可引用公开知识来源、可执行 Agent 任务和效果反馈。抖音现已区分短视频脚本与小店商品文案：脚本按完整分镜强校验，商品文案按标题、详情与卖点段落独立校验。跨平台生成请求会先要求用户选择转换为当前平台或新建目标平台会话。

产品范围已重新聚焦内容与运营闭环：商品图工作室从欢迎页、导航、会话恢复、平台选择和前端代码中下线；后端 Studio/视觉实验代码暂时保留，后续重新立项前不作为当前能力维护或验收。

2026-06-17 完成多用户鉴权隔离基础设施：生产可通过 `SHOPGENIE_AUTH_TOKENS_JSON` 配置 Bearer token 到稳定用户 ID 的映射；本地未配置 token 时继续兼容 `X-User-Id` 开发切用户。品牌档案、商品、会话、内容资产/版本、知识源、任务、效果数据、A/B 实验、运营建议状态、CSV 导入、平台连接器与生成 Prompt 上下文均按 owner 隔离。已确认线上 SSH 可达，nginx 与 8000 后端健康；线上仍为手动 uvicorn 进程，且生产仓库 ahead `origin/main` 3 个提交。

主干已切换为 `main`（GitHub 默认分支，`feat/frontend-v3-polish` 同步跟随）。**V2 运营增长闭环已落两个增量**：①「评论反哺」粘贴买家评价→提炼洞察（认可/顾虑/原声/踩雷）→注入生成（已上线生产）；②「A/B + 效果回写闭环」一键生成标题/钩子变体→回填曝光转化→判定赢家→赢家风格反哺生成（已合并 main，待部署）。后端 83 测试通过。服务器改为 git over SSH 只读部署密钥同步。

2026-06-10 完成三轮修复：① Studio bug 修复轮（会话隔离、首次输入失效、vision content 解析、OSS URL 过期、消息 ID 冲突、微调闭包覆盖、模板 prompt_template 接通、轮询契约统一、空请求拒绝）；② P0/P1 验证修复轮（API 路由 404、平台强契约+自动矫正、流式链路一致性、STUDIO 422、图片限制统一、移动端 Studio 导航、场景单商品 Prompt、历史脏数据修复）；③ 前端成熟度修复轮（统一 toast 错误提示、错误边界、Studio 任务可恢复/可取消、会话自动命名、Esc 关弹层、触屏删除常显、草稿持久化），全程 curl/真实 API/浏览器实测验证。**Studio 前端完整链路已真实验收通过**（真实万相调用：三视图生成→调整→取消→刷新→恢复→结果落地）。

⚠️ 仍待验收：生成图片的商品一致性质量抽检（三视图视角区分度、场景图单商品）。详见"遗留与待办"。

---

## 里程碑进度

### M0：项目搭建
- [x] PRD 定稿 (v1.0)
- [x] 通用工程规则引入
- [x] 项目规则 AGENTS.md（草稿）
- [x] 竞品调研文档
- [x] V3 UI 设计稿（3个版本，选定V3）
- [x] Next.js 前端项目搭建
- [x] 设计稿转前端代码（组件化，零报错）
- [x] V3 前端视觉重做（大屏尺度、结果卡、输入区、移动端）
- [x] 前端关键演示交互（发送消息、Enter 发送、复制全文、移动端导航）
- [x] Amazon 前端平台入口（平台切换、会话示例、欢迎页入口）
- [x] Git 仓库初始化并创建功能分支
- [x] FastAPI 后端项目搭建

### M1：对话 + 脚本生成
- [x] 后端 API 框架
- [x] LLM 对话接口（DeepSeek V4 Flash）
- [x] 抖音短视频脚本生成（首版 Prompt）
- [x] Web 端真实生成闭环

### M2：小红书 + Amazon + 后处理
- [x] 小红书种草笔记生成
- [x] Amazon Listing 生成（英文标题 + Bullet Points + 商品描述）
- [x] 三平台 Prompt 规则与基础合规检查
- [x] 广告法、平台与品牌自定义违禁词检测
- [x] 标签推荐

### M3：前端 + 记忆
- [x] Chat UI 完善
- [x] 用户档案系统
- [x] 偏好记忆

### M4：内测
- [ ] 找真实商家试用
- [ ] 收集反馈
- [ ] 迭代

---

## 执行日志

### 2026-06-17（多用户鉴权隔离 + 线上 SSH 状态）
- 新增 `auth.py`：配置 `SHOPGENIE_AUTH_TOKENS_JSON` 后，除 `/health` 外的 API 需要 `Authorization: Bearer <token>`；token 映射到稳定 `owner_id`。未配置时保留本地开发模式 `X-User-Id`，避免打断单机 demo 与测试。
- SQLite 业务表新增/迁移 `owner_id`，旧数据归属 `default`；商品、档案、会话、内容资产/版本、知识源、Agent 任务、效果数据、A/B 实验和运营建议状态全部按 owner 读写。会话 ID、内容/效果记录等跨用户冲突不再允许静默覆盖。
- 生成链路补齐隔离：普通聊天、流式聊天、批量生成、内容修订、Agent 任务、知识检索、历史效果、A/B 赢家和内容历史 Prompt 均只读取当前 owner 数据。
- 前端 API 统一鉴权头：有 `auth_token`/`NEXT_PUBLIC_SHOPGENIE_API_TOKEN` 时发送 Bearer token，否则继续发送开发用 `X-User-Id`。
- 新增最小登录系统：`/api/auth/register` 支持账号密码注册（不验证邮箱），密码以 PBKDF2 哈希保存；`/api/auth/login` 支持账号密码登录并返回签名 token，兼容旧访问码；`/api/auth/me` 校验当前 token。前端 `AuthGate` 在未登录时显示登录/注册页，登录后再挂载应用，退出会清本地 token、草稿和会话缓存。
- 线上 SSH 状态：`root@47.99.131.123` / `root@liujufu.com` 可登录；nginx 80/443 正常，uvicorn 8000 正常，`/health` 本机与公网均 200；服务器到 GitHub SSH 正常。风险：线上 `/var/www/shopgenie` ahead `origin/main` 3 个提交，存在未跟踪 `backend/app/test_ab_testing.py`、`backend/app/test_review_mining.py`、`backend/uvicorn.out`；后端仍是手动 uvicorn 非 systemd/pm2。
- 验证：后端 `129 passed`；前端 `45 passed`；前端生产 build 通过。

### 2026-06-15（抖音入口拆分、首屏加载与品牌图标）
- 欢迎页抖音快捷创作拆为“抖音短视频脚本”和“抖音商品文案”，分别预填明确需求并复用后端独立内容类型契约。
- 修复页面恢复服务端会话后，把所有历史会话误判为脏数据并逐条回写的问题；刷新时不再产生一串无意义的 `POST /api/sessions`。
- 浏览器 favicon 替换为 ShopGenie 品牌图标。
- 生产 nginx 为带内容哈希的 `_next/static` 构建资源增加一年 immutable 缓存并启用 gzip，减少跨地域重复传输；HTML 保持不缓存，确保部署后及时更新。
- 验证：前端 `41 passed`；lint `0 errors`；生产 build 通过；浏览器确认两个抖音入口、商品文案预填与新图标生效，刷新只读取会话、不批量回写；线上静态资源返回长期缓存头与 gzip。

### 2026-06-15（PRD 同步与商品图工作室前端下线）
- PRD 同步当前真实能力、V3/V4 部分完成状态、实际技术架构与当前产品边界。
- 删除前端商品图工作室视图、图片生成面板、API 调用和入口；历史 Studio 会话恢复时自动过滤。
- `studio` 仅作为后端和历史数据兼容枚举保留，品牌档案、知识来源等所有用户可操作的平台选择均不再展示。
- README 与 AGENTS 模块说明同步为“后端实验代码暂存，当前产品不开放”。
- 验证：后端 `124 passed`；前端 `40 passed`；lint `0 errors`（7 个既有 warnings）；生产 build 通过并生成 8 个业务静态页面；浏览器确认欢迎页、品牌档案和知识来源平台下拉均无 Studio 入口，控制台无错误。

### 2026-06-12（阶段 1：UI 与体验缺陷修复）
- 统一收敛 CSS Design Token，并在 `:root` 中声明字阶（5级）、圆角（3级）、重点色（合并为柔珊瑚橙 `--accent`）与语义色（success/warning/danger）。全站样式收拢对齐。
- 优化客服话术结果预览卡，为每个客服场景/变体区块增加“复制此话术”（仅复制单个变体）及“微调”操作（展开输入框并通过聊天发送微调请求）。
- 改造侧栏最近对话按钮为独立可聚焦的双 Button 结构（div 容器 + 访问按钮 + 删除按钮），消除嵌套交互元素并支持 Tab 键及 Focus 状态删除按钮常显。
- 调整欢迎页 Studio 生图描述文案，使其与“三视图生成 → 调整外观 → 场景图生成”实际流程一致。
- 验证：前端所有 31 个测试 100% 通过；生产环境下 static pages 顺利 build 成功，eslint 无 error，警告减少至 12 个。

### 2026-06-12（商品上下文强契约）
- ⚠️ 行为变化：应用不再默认使用商品库第一条；查看商品详情不再切换当前生成商品；会话产生真实消息后商品上下文锁定，切换商品需要新建会话。
- 评论洞察新增所属 `product_id` 与商品名；只有洞察归属与当前商品完全匹配时才注入生成 Prompt。无归属的旧洞察停止注入并提示重新分析。
- 普通与流式聊天收到不存在或已删除的商品 ID 时明确拒绝，不再静默按无商品生成。
- 商品库新增显式“用于当前生成”动作；创建商品不再自动改变生成上下文。
- 项目规则与 PRD 新增商品强会话契约，后续商品事实、评论洞察、历史效果和内容资产都按同一有效商品 ID 隔离。
- 验证：后端 `102 passed`；前端 `30 passed`；生产 build 通过；lint `0 errors`。

### 2026-06-12（历史错绑商品与追问链路修复）
- 截图复现确认：“儿童智能手表 Amazon”历史会话被旧版自动默认逻辑错误绑定到“玻尿酸面膜”，因此跨商品显示评论洞察。
- 会话持久化新增 `product_binding_confirmed`；旧会话没有用户确认凭据，迁移时统一清除商品绑定。新会话只有显式点击“用于当前生成”才保存绑定。
- 评论洞察提示条增加归属校验，旧版无归属洞察不再显示为“正在参考”。
- 多问题追问要求每题都回答后才允许提交，提交内容包含完整“问题：答案”列表。
- DeepSeek 返回结构不完整的草稿时保留引导语和追问继续收集信息，不再直接显示红色成品结构错误；最终成品结构不完整仍明确报错。
- 验证：截图中的历史手表会话商品绑定已从数据库清除；后端 `105 passed`；前端 `31 passed`；生产 build 通过；lint `0 errors`。

### 2026-06-12（A/B 最小样本量与置信提示：后端完成）
- ⚠️ 行为变化：原来任意变体有少量曝光就可能立即宣布赢家；现在要求所有变体分别达到至少 300 次曝光，未达标时保持投放中且不反哺后续生成。
- 实验契约新增 `confidence_level` 与 `confidence_message`，明确返回样本是否足够、合计还差多少曝光，并声明确定性门槛不代表统计显著性。
- 保存和读取实验时都会重新执行门槛判断，历史上过早判定的赢家不会继续进入生成上下文。
- 录入实验指标新增漏斗校验：点击不能大于曝光，转化不能大于点击；模型不足 2 个变体时拒绝创建不可用实验。
- 指挥台会持续提醒仍在补样本的实验，不再只提醒完全没有曝光的实验。
- 前端仅同步新增字段的 TypeScript 契约，置信提示 UI 留到后续前端优化批次。
- 验证：后端 `99 passed`；前端 `27 passed`；生产 build 通过。

### 2026-06-12（前端优化方向记录，暂缓实施）
- 已记录后续前端优化方向：收敛玻璃材质、首页运营化、工作台一级导航重排、查看商品与生成上下文解耦、统一状态语言、强化 ShopGenie 品牌识别。
- 当前决策：先推进后端运营能力，前端方向保留在 PRD 4.4，暂不修改界面。

### 2026-06-11（商品库详情交互修正）
- ⚠️ 行为变化：商品库右侧不再常驻“新建商品”表单；默认展示选中商品的完整详情与编辑入口，新建商品改为左侧显式按钮触发。
- 点击左侧商品会同时查看详情并设为当前生成商品，避免“查看 A、生成却使用 B”的隐性状态；列表明确标记正在用于生成的商品。
- 商品详情可编辑商品名、品类、目标人群、核心卖点、确认事实、禁止声明和运营备注，保存走已有 `PUT /api/products/{id}` 契约；评论反哺继续跟随当前商品详情。
- 验证：新增 `ProductsTab` 交互与更新契约测试；前端 lint、build 通过；本地浏览器走查商品详情与显式新建流程通过。

### 2026-06-11（CSV 数据导入 + 完整运营漏斗）
- PRD 新增完整“运营能力演进路线”：记录 CSV 完整漏斗、A/B 样本门槛、内容日历、评论预警、竞品监控、跨商品看板、平台连接器和自动发布的价值、成功指标、依赖、边界与状态；下一项明确为 A/B 最小样本量与置信提示。
- 效果数据契约新增加购、下单、退款、广告花费，汇总 CTR、点击后成交率、退款率和 ROAS；保留原 `conversion_rate` 口径，避免静默破坏旧契约。
- 新增 `performance_import.py` 与 CSV 预览/导入 API：要求真实内容资产 ID，非负数字校验，明确错误行号，单次最多 500 行；所有行通过后才在一个 SQLite 事务中整批写入。
- 效果页新增完整漏斗 KPI 卡、CSV 文件/文本输入、预览校验、确认导入和可用内容资产 ID；确认导入前不会写数据。
- 指挥台使用下单口径诊断点击后成交，并新增下单达到 10 且退款率高于 10% 的高优先级提醒。
- 验证：后端 `95 passed`；前端 `25 passed`；lint 通过；生产 build 通过；桌面端与 390×844 移动端浏览器走查通过；真实 CSV 预览校验成功且未确认写入。

### 2026-06-11（每日运营指挥台首版）
- PRD 升级到 v1.1，新增每日运营指挥台目标、确定性诊断规则、展示要求与首版安全边界。
- 新增 `backend/app/operations.py` 与 `GET /api/operations/brief`：覆盖首次建商品、缺评论洞察、缺效果回流、曝光高点击低、点击高转化低、A/B 实验待投放等行动建议；按优先级排序，最多返回 5 条；健康状态不制造虚假任务。
- 首页运营台升级为“今日运营指挥台”：展示诊断摘要、状态、行动原因、关键指标和工作台直达入口。
- 效果回填补充“点击量”，使 CTR 与点击后转化诊断具备真实 UI 数据来源。
- 测试新增：后端运营建议优先级、空数据与健康状态、API 返回；前端行动展示与目标工作台跳转。
- 验证：后端 `89 passed`；前端 `24 passed`；lint 通过；生产 build 通过；桌面端真实点击行动卡直达效果数据；390×844 移动端走查通过。

### 2026-06-10（续8：批量生成 — 一个商品一键铺满全平台）
- 新功能：选一个商品/写一句描述 + 勾平台 → 并行产出多平台内容,各自存为内容资产。运营规模化的核心效率工具。
- 后端：`POST /api/batch/generate` 用 `asyncio.gather` 并行调用现有 `DeepSeekClient.chat` 生成管线(每平台一条,质量与单条一致),平台去重 + 剔除 studio,空平台 422、商品不存在 404;`test_batch.py` 4 个测试。
- 前端：首页「🚀 一键全平台批量生成」深色 CTA → `BatchView`(商品下拉 + 描述 + 平台多选 + 生成),生成中每平台显示骨架,结果用**各平台原生 ResultCard 并排展示**,失败平台单独提示不影响其他。新增 view 'batch' + topbar 返回。
- 验证：后端 87 测试通过(+4);tsc clean;lint 0 error;前端 vitest 23;build 通过;浏览器真实并行实测——小红书 + 抖音两平台同时生成,各自渲染原生预览卡。
- 部署：本轮起的 A/B 闭环、运营 Hub、上下文嵌入、客服重做、串商品修复**已全量部署到生产并公网验证**(站点/API 200、A/B 路由经 nginx 命中);批量生成已合 main,待下次部署。

### 2026-06-10（续7：运营能力露出 — 首页 Hub + 上下文嵌入）
- 问题：评论反哺、A/B 实验、效果数据等核心运营能力全埋在「内容工作台」模态抽屉的 tab 里，用户打开 App 看不到、也猜不到，等于白做。
- ① 首页运营台 Hub：欢迎页平台卡下方新增「你的运营台」——商品库 / 🧪 A/B 实验 / 📈 效果洞察 三张卡，**带实时状态**（如"1 个商品·1 个已分析评论""0 投放中·1 已决出""转化率 X%"），点击直接深链进工作台对应 tab。
- ② 工作台深链：`WorkspacePanel` 导出 `WorkspaceTab` 类型 + 新增 `initialTab` prop，打开时跳到指定 tab；侧栏入口默认回 `products`。
- ③ 上下文嵌入：聊天输入框上方，当当前商品已有评论洞察时显示绿色提示条「💬 正在参考评论洞察：xxx · 管理 →」，让用户**看见系统正在用他的评价数据**，点击进工作台管理。
- 验证：tsc clean；lint 0 error（清掉 2 个未用导入）；前端 vitest 23；build 通过；浏览器实测——Hub 实时数据正确、点 A/B 卡直达实验 tab、洞察条在选中已分析商品时正确显示卖点。

### 2026-06-10（续6：A/B + 效果回写闭环 — 运营增长闭环第二个增量）
- 新功能：同商品同平台一键生成多个标题/钩子变体 → 真实投放回填曝光/点击/转化 → 按转化率判定赢家 → 赢家风格反哺后续生成。把"生成"和"发布效果"连成数据驱动迭代闭环。
- 后端：
  - `ab_testing.py`：`generate_variants()` 调 DeepSeek 产出 N（2-5）个**策略明显不同**的标题+钩子变体（痛点/好奇/数字/场景/反差），带 A/B/C 标签和清零指标；空输入 422。
  - `Experiment` 数据模型 + `experiments` 表 + CRUD；`compute_winner()` 按转化率判定（同率比点击再比曝光），无数据保持 running。
  - `build_experiment_prompt()` 把已决出的赢家标题/钩子注入 `chat()` 与 `chat_stream()` 两条管线（仅注入赢家、按平台过滤）。
  - 路由：`GET /api/experiments`、`POST /api/experiments/generate`、`POST /api/experiments/{id}/metrics`（录入即重算赢家）、`DELETE`；studio 平台拒绝 422。
- 前端：工作台新增「🧪 A/B 实验」Tab——选平台+对比方向→生成 3 变体→每个变体卡展示策略角度/标题/钩子 + 曝光/点击/转化录入 + 实时转化率；赢家自动高亮（🏆 + 绿色描边 + "赢家 X"状态）。错误走统一 toast。
- 测试：`test_ab_testing.py` 6 个（赢家判定、无数据保持 running、赢家注入 prompt+平台过滤、变体生成标签/清零、空输入 422、API 全流程含 studio 422/404/删除）。后端 83 测试通过。
- 验证：tsc clean；lint 0 error；前端 vitest 23；build 通过；浏览器真实 DeepSeek 实测——生成 3 个差异变体（好奇/场景/数字角度）→ 录入指标 → B 以 3.50% 转化率被判赢家并高亮（截图存档）。

### 2026-06-10（续5：评论反哺 Review Mining — 运营增长闭环首个增量）
- 新功能：从真实买家评价提炼结构化洞察，反哺内容生成（把"模型臆想卖点"升级为"用户真实在乎的卖点"）。
- 后端：
  - `review_mining.py`：`analyze_reviews()` 调 DeepSeek 提炼 `loved_points/pain_points/avoid_phrases/voice_quotes/summary`，`normalize_insights()` 去重去空限长；评论上限 20000 字符，空输入抛 422。
  - `DeepSeekClient.complete_json()`：通用 JSON 补全方法，供非内容生成的结构化抽取复用。
  - `Product.review_insights` 字段（dict|None，向后兼容旧记录）。
  - `build_review_prompt()` 注入 `build_product_prompt`，聊天与流式两条管线自动生效；普通更新商品不抹掉洞察（ProductInput 不含该字段）。
  - 路由：`POST /api/products/{id}/reviews/analyze`（分析+存储）、`DELETE /api/products/{id}/reviews`（清除）。
- 前端：工作台「商品库」Tab 选中商品后出现「💬 评论反哺」面板——粘贴评价→一键分析→色彩分组展示洞察（认可/顾虑/原声/踩雷）+ 一句话总结 + 清除；错误走统一 toast。
- 测试：`test_review_mining.py` 6 个（规整去重、prompt 注入、analyze 规整、空输入 422、API 全流程含"更新不抹洞察"+404）。后端 77 测试通过。
- 验证：tsc clean；lint 0 error；前端 vitest 23 通过；浏览器真实 DeepSeek 实测——建商品→粘贴 6 条面膜评价→分析返回结构化洞察→刷新后面板正确渲染（截图存档）。
- 分支与上线：本轮起以 `main` 为主干（新建并设为 GitHub 默认分支，`feat/frontend-v3-polish` 同步跟随）。评论反哺已**全量部署到生产**——服务器 `git reset --hard origin/main`、`npm run build` 重建 `frontend/out`、重启 uvicorn 后端加载新路由；公网实测：站点 200、API 200、`/api/products/{id}/reviews/analyze` 经 nginx 命中后端（假 id 返回业务 404「商品不存在」而非 Next 静态 404，证明路由已注册并代理正确）。
- 安全：服务器 git 远程地址此前内嵌明文 `gho_` token，已改为 SSH 只读部署密钥（repo-scoped，`gh repo deploy-key add`）；旧 token 待用户撤销。

### 2026-06-10（续4：解析容错 + Prompt 结构强化）
- DeepSeek 解析全面容错，不再因模型输出格式问题炸用户：
  - 非 JSON 内容降级为闲聊（返回前 2000 字），不再 `raise DeepSeekError`。
  - 未知 `kind`：优先用 title/body 字段拼装预览卡片（仍走平台强契约校验），拼装失败再降级为闲聊。
  - `_try_parse_json` 增加多策略解析链：直接解析 → 去尾逗号（`,]`/`,}`）→ 单引号转双引号 → 组合修复；对截断 JSON 的子串扫描同样套用全部策略。
  - 记录原始 content preview 日志（前 500 字）便于排查模型输出问题。
- Prompt 结构强化（`prompts.py`）：
  - 小红书：明确 sections 数组用于正文分段（最多 3 段，label 小标题 + content 正文，可为空）。
  - Amazon：加"语言红线"——title/body/tags/sections 全英文、message 中文，违反会被后端拦截；Bullet Points 明确用 sections 数组（≥5 条），附 sections 示例格式。
- 流式链路自动受益：`stream.py` 复用 `_parse_content`，同样获得降级容错（非 JSON/未知 kind 不再中断 SSE）。
- 验证：后端 71 测试通过。

### 2026-06-10（续3：前端成熟度修复轮，真实万相调用实测）
- 统一错误提示：新增模块级事件总线 `lib/toast.ts` + `Toaster` 组件（`role=status aria-live=polite`、可手动关闭），替换全部 `alert()`/内联 error/静默吞错；移除 StudioView/ProfilePanel 的固定错误条。
- 新增应用级错误边界 `app/error.tsx`（重试 reset + 刷新页面），白屏崩溃可恢复。
- Studio 任务可恢复 + 可取消：`pendingTask`（task_id + kind）持久化进 `conversation.studio`，付费万相任务提交后即落盘；`pollUntilDone` 支持 `isCancelled` 回调，每 2s tick 检查；"停止等待"取消轮询但任务仍在云端执行；刷新/切换会话后显示"继续等待/放弃"恢复横幅。**真实万相调用全链路实测**：提交→取消→刷新→恢复横幅→继续等待→结果落地（adjHistory 正确追加、pendingTask 清空）。
- 会话自动命名：成品 `result.title` 替换截断式临时标题（24 字截断 + `named` 标记，已命名会话不被覆盖）。
- 弹层可访问性：新增 `useEscClose` hook，ProfilePanel/WorkspacePanel 支持 Esc 关闭（浏览器实测）。
- 触屏删除常显：`@media (hover: none)` 下会话删除按钮始终可见。
- 草稿持久化：聊天输入草稿与 Studio 描述草稿写入 sessionStorage，刷新不丢（规避 set-state-in-effect 用 `Promise.resolve().then`）。
- `repairStudio`：hydrate 时复位所有 `tweaking` 标记，避免刷新后卡在"调整中"。
- 验证：后端 71 测试通过；前端 vitest 23 通过；tsc clean；lint 0 error / 15 warnings；build 通过；桌面 + 390×844 浏览器 E2E 走查通过（真实生成黑色保温杯三视图并展示）。

### 2026-06-10（续2：P0/P1 验证修复轮，全程实测验证）
- 修复 `/shopgenie/api/studio/*` 404：本版 Next.js rewrites 的 source 会自动加 basePath 前缀，`next.config.ts` 中 source 改为 `/api/:path*`；curl 实测 templates/sessions/profile 经 3000 端口全部 200。
- 平台强契约落地：`enforce_platform_contract` 校验失败→自动矫正一次（带错误清单的第二次 LLM 调用）→仍失败则拦截（result=None、message 含"已拦截"、🚫 warnings），非流式与流式链路统一接入；被拦截内容不再保存为资产。
- ResultCard 状态真实化：仅在后端契约通过时展示"✓ 平台结构校验通过"，自动矫正过的成品额外显示"⚠ 已自动矫正一次"。
- 流式链路补齐：接入品牌禁忌词 post_process 与 `retrieve_knowledge` 参考来源（此前 sources 硬编码 `[]`）；真实 DeepSeek 流式调用实测 status/token/result/done 事件齐全、sources 4 条、quality 80。
- `Platform.STUDIO` 不再进入聊天链路：`/api/chat` 与 `/api/chat/stream` 对 studio 平台返回 422（实测验证），消除 PLATFORM_PROMPTS KeyError 风险。
- 统一图片大小限制：InputBar 与 StudioView 均按 base64 编码后长度（5_000_000）校验，与后端一致，错误文案明确提示压缩到约 3.5MB。
- 移动端 Studio 导航：Studio 视图增加 topbar（菜单按钮 + studio 平台徽标 + 标题 + "← 返回"），390x844 浏览器实测：导航→新对话→工作室卡片→返回欢迎页全程可达，用户不再被困。
- 场景图单商品 Prompt 加强：明确"只提取正面视角那一件商品，画面中有且仅有一件，禁止出现其他视图副本"。
- 修复历史脏数据重复消息 ID：hydrate 时 `repairMessageIds` 对后端恢复和 localStorage 两条路径去重（旧版 Studio ID bug 留下的 `message-133` 重复 key，浏览器 console 实测不再报错）。
- Studio 空状态文案去除"在左侧"方位词（移动端为上下布局）。
- 新增 `tests/test_platform_contract.py`：自动矫正成功、矫正失败拦截、studio 平台 422，共 3 个测试。
- 验证：后端 71 测试通过；前端 23 测试通过；lint 0 error；build 通过；移动端浏览器（390x844）走查通过。

### 2026-06-10（续：Studio bug 修复轮）
- 修复 Studio 入口会话隔离：进入 Studio 时清空 activeId；StudioView 只认 `platform === 'studio'` 的会话，杜绝 Studio 状态/消息写进聊天会话。
- 修复首次进入 Studio 输入框失效：无会话时用本地草稿状态（localStudio），首次生成时草稿随 ensureConv 带进新会话。
- 修复 `analyze_image` 返回 list：DashScope 原生端点 content 是 `[{"text":...}]` 列表，新增 `extract_content_text` 提取文本；此前 `/api/vision/analyze` 必 502。
- 接通 `_to_b64`：submit_adjust/scene/tweak/product_view 对 URL 参考图统一下载转 base64，修复恢复旧会话后 OSS 签名 URL 过期导致的必败。
- 修复 StudioView 消息 ID 冲突：改用 `studio-{timestamp}-{n}`，不再与 useChat 的 `message-N` 撞号，重新挂载也不重复。
- 修复 handleTweak 闭包覆盖：setState 回调内按 URL 匹配最新 results，微调期间新生成的场景图不再被丢弃。
- 场景模板实际使用 `prompt_template`：选模板后以模板 prompt 为基础、自定义描述为补充，不再发送无意义的 `Template: {id}`。
- 统一轮询契约：后端 `poll_task` 改为单次查询（不再服务端阻塞循环），前端负责轮询和 120s 总超时；前端轮询遇 408/502 或连续 3 次异常立即停止。
- 后端拒绝空描述+空图片的生图请求（422），避免无效 API 成本。
- 修复 lint error（WorkspacePanel set-state-in-effect），lint 恢复 0 error（剩 15 个低优先级 warnings）。
- persist 防抖从 500ms 恢复为 1.5s（之前优化被意外回退）。
- 新增 `tests/test_studio_api.py`：空输入 422、无 API Key 503、字段校验、content 提取，共 6 个测试。
- 验证：后端 68 测试通过；前端 23 测试通过；lint 0 error；build 通过。

### 2026-06-10
- 交互逻辑修复：选平台不再立即创建会话，发第一条消息后会话才出现在侧栏。
  - 新增 `pendingPlatform` 状态，`startFromPlatform` 不调 `createConversation`
  - `send()` 增加 `forcePlatform` 参数，首次发送时创建会话
  - 修复重复创建会话 bug：`send()` 提前设 `pending=true`
- AI 生图入口重做：从浮动橙色按钮改为 WorkspacePanel 风格右侧滑出面板。
- 砍掉「设计助手」入口，替换为「📸 商品图工作室」——独立全屏工具页。
  - 新增 `StudioView` 组件：左栏上传+自动抠图预览，右栏场景模板+生成结果
  - 后端新增 `studio.py`：`/api/studio/remove-bg` (rembg 抠图) + `/api/studio/generate` (通义万相换背景)
  - 移除 `Platform.DESIGN`，新增 `Platform.STUDIO`
  - 移除 `ImageGenPanel` 组件及关联 CSS
- SOUL.md 加硬规则：方括号标签只出现在 TTS 命令中，聊天文字禁止。
- 文档同步：TEST_PLAN.md 交互测试用例。
- 验证：后端 62 测试通过；前端 23 测试通过；build 通过。

### 2026-06-09（续2）
- 平台强契约：新增 platform_validator.py，三平台确定性结构校验器。
  - 小红书：必须中文标题+正文+至少1个标签，拒绝英文内容污染。
  - 抖音：必须中文，需要时间分镜结构或至少2个段落。
  - Amazon：必须英文标题+正文+至少2条Bullet Points。
  - 跨平台污染检测：Amazon内容出现在XHS会话会被标记。
- 校验器集成到 deepseek.py 和 stream.py 生成管线，失败时以 🚫 前缀加入 warnings。
- 15 个平台校验测试新增，总测试 62 个通过。
- 会话持久化：新增 sessions.py，SQLite 存储聊天会话（id/platform/title/product_id/messages）。
- 后端 GET/POST/DELETE /api/sessions 端点。
- 前端 useChat 改为优先从后端加载会话，后端不可用时降级到 localStorage。
- 删除会话同步删除后端和 localStorage。
- 前端测试框架：安装 Vitest + React Testing Library，新增 6 个前端测试通过。
- 遗留待办更新：平台强契约、跨平台回归测试、会话持久化、前端测试框架四项已清除。
- 验证：后端 62 测试通过；前端 6 测试通过；build 通过。
- 前端 6 项 UX 优化：Cmd+Enter 发送、复制按钮绿色反馈、工作台锁滚动、品牌档案提示可点击、persist 防抖 1.5s、确认 regenerate 按钮已存在。
- 验证：前端 6 测试通过；build 通过。
- RAG 关联：build_content_history_prompt 将同商品同平台的已有内容资产最新版本注入生成上下文。
- 后续生成参考已有版本的表达风格和卖点角度，不再完全独立生成。
- 集成到 chat() 和 chat_stream() 两条管线。
- 验证：后端 62 测试通过；前端 build 通过。
- DashScope API 集成：Qwen-VL 图片理解 + 通义万相图片生成。
- 设计助手场景：拍摄指导、主图文案、详情页结构，黄色主题。
- 图片上传功能：输入框图片按钮、预览条、base64 传输、Qwen-VL 分析注入上下文。
- 修复 stream.py quality.model_dump() 报错（quality 已是 dict）。
- 服务器部署：vision.py、设计场景、图片上传全部同步。
- 验证：后端 62 测试通过；前端 build 通过。
- 竞品分析闭环：competitive_analysis.py，生成前自动搜索同类爆款内容。
- 搜索 DuckDuckGo 获取竞品标题和摘要，提取规律注入生成上下文。
- _extract_category 从用户消息中启发式提取品类关键词。
- 集成到 chat() 和 chat_stream() 两条管线，搜索失败时优雅降级。
- 前端组件测试补充：InputBar（7个）+ ChatBubble（10个），覆盖渲染、交互、状态、警告、占位符。
- 总测试：后端 62 + 前端 23 = 85 个通过。
- 新增客服话术场景：Platform.CS，售前+售后话术模板生成。
- 欢迎页新增"更多场景"区域：客服话术卡片，独立于三个平台卡片。
- CS 平台紫色主题：pill、对话圆点、平台标签。
- 验证：后端 62 测试通过；前端 23 测试通过；build 通过。
- 知识库种子数据：seed_knowledge.py 预填 10 条三平台核心规则。
  - 小红书：审核红线、爆款笔记结构、SEO策略。
  - 抖音：脚本规范、违禁词规则、算法和发布时间。
  - Amazon：Listing优化规范、SEO策略、常见违规。
  - 通用：内容创作原则 + 禁忌词列表。

### 2026-06-09（续）
- 前端重构：page.tsx（312 行）拆分为 useChat hook（220 行）+ page.tsx（165 行）。
- useChat 包含：会话状态管理、消息收发、AbortController、regenerate、localStorage 持久化。
- page.tsx 只负责渲染 + UI 状态，不再包含聊天业务逻辑。
- AGENTS.md 模块结构已同步。
- 验证：后端 47 测试通过；前端 build 通过。
- ⚠️ 已知问题：localStorage 恢复会话时 idCounter 初始化可能与已有消息 ID 冲突，导致 React key 重复警告。
- 修复 React key 冲突：idCounter 从 localStorage 恢复时读取实际最大 message ID，不再用计数+100。
- 新增删除对话功能：侧栏 hover 显示 × 按钮，点击两次确认删除；删除活跃会话自动切换到下一条。
- 新增 SSE 流式输出：后端 /api/chat/stream 端点，DeepSeekClient.chat_stream() 实时转发 token。
- 新增 Agent 状态指示：pending 气泡实时显示状态文字（正在加载上下文→正在发现知识→正在生成内容→正在解析→正在质检）。
- 前端 sendChatStream() 异步生成器，useChat 改用流式消费。
- 删除对话改为弹窗确认：点击 × 弹出模态对话框，显示对话标题，确认/取消。
- 新增版本对比视图：VersionDiff 组件，基于 LCS 的词级别 diff，绿色高亮新增、红色高亮删除。
- 验证：后端 47 测试通过；前端 build 通过。

### 2026-06-09
- ⚠️ 人工测试发现跨平台格式污染：Amazon 会话可被外部 Prompt 带成中文笔记，抖音脚本也可能退化为小红书式正文。
- 产品决策：会话平台升级为强契约；成品展示前必须执行确定性平台结构校验，不合格时自动矫正一次，仍失败则不展示错误成品。
- 文档已补充多平台扩展优先级：先修稳抖音、小红书、Amazon，再优先接入 TikTok Shop、视频号、淘宝/天猫；本次仅完成规则定义，尚未实现。
- 完成 V1.5 闭环审计并补齐弱环节：同商品同平台的历史效果现在会注入后续生成，不再只做数据展示。
- 知识来源改为按当前需求相关性检索，并随回复返回可点击来源；知识与商品事实严格分区，避免把平台规则误当商品事实。
- 新增安全公开网页导入：仅允许公开 HTTP/HTTPS 文本网页，拒绝本机/内网、账号 URL、非文本和超过 1MB 的响应。
- 新增有界公开网页发现：用户明确要求“最新 / 竞品 / 趋势 / 搜索 / 调研”时 Agent 先发现并保存最多 5 个公开来源；工作台也可主动发起发现。
- Agent 任务从“只记录计划”升级为可针对内容资产执行优化、创建新版本和重新质检；模型失败时任务明确标记 failed。
- 多轮对话修复：AI 历史现在包含上一版结构化成品；商品绑定到单个会话，切换会话不再串商品。
- 质量检查新增未经商品事实库确认的规格数字检测；手动版本同样执行合规检查。
- 拆分 `workspace_context.py`，保持工作台存储模块低于单文件 soft cap。
- 修复移动端无法进入内容工作台：侧栏新增工作台入口，390×844 视口下完成打开与表单可访问回归。
- 验证：后端 47 个测试通过；真实公开网页发现返回 5 条结果；前端 lint/build 通过；桌面和移动端浏览器完成知识发现与 Agent 执行入口回归，控制台无错误。
- 新增 V1.5 内容工作台：商品库、内容资产与版本、知识来源、Agent 任务、发布效果五个可操作模块。
- 商品事实库接入生成 Prompt；用户通过 `事实：`、`卖点：`、`禁止：` 明确标注的信息会写回当前商品，普通对话不会被误记。
- 每次生成成品自动保存为内容资产和 v1 版本；支持手动编辑保存新版本、局部 AI 改写和版本历史。
- 新增确定性质量评分：标题、正文、待补充项、合规警告、商品禁用声明和平台结构；支持按质量建议自动修订。
- 新增知识来源库，按平台将已验证规则注入生成；当前由用户录入，尚未联网自动搜索。
- 新增轻量 Agent 执行计划记录；新增发布曝光/转化录入与整体转化率汇总。
- 内容结果卡新增质量分与“编辑与版本”入口；聊天顶部展示当前选中的商品事实。
- 验证：后端 34 个测试通过；前端 lint/build 通过；浏览器完成工作台、商品选择、内容版本、质量报告、效果页回归，控制台无错误。
- 修复左下角品牌资料固定显示 `XX美妆`：改为可点击的品牌档案入口，空档案显示设置引导，保存后侧栏立即同步品牌名、品类、调性、偏好和常用平台。
- 品牌档案新增常用平台选择、保存失败提示与输入边界校验；修复多偏好/禁忌词逐字输入时分隔符被吞的问题；SQLite 访问从 async API 主路径移入线程池，避免阻塞事件循环。
- 品牌自定义禁忌词接入确定性后处理，生成结果命中时返回明确警告。
- 修复重新生成重复追加用户消息、携带旧 AI 回复作为历史的问题；停止生成显示明确中文状态。
- 修复 Markdown 包裹 JSON 时会话标题和追问选项丢失的问题。
- 清理无效前端控件：结果卡“再来一版”接入真实重新生成，移除无功能的编辑、历史和分享按钮。
- 结果预览与新对话页改为使用真实品牌档案；未设置档案时使用明确占位提示，不再伪装成固定商家资料。
- 增加档案 API 边界测试、自定义禁忌词测试和 Markdown JSON 解析回归测试。
- 验证：后端 26 个测试通过；前端 lint/build 通过；浏览器完成档案保存、刷新、侧栏同步、新对话与结果预览回归，控制台无错误。

### 2026-06-08（续）
- ⚠️ Prompt 策略调整：原"少于 3 项产品事实禁止生成"规则过于严格，用户仅输入品类时被反复追问无法预览，体验差。
- 重写 BASE_PROMPT：三级生成策略（信息充足→直接成品 / 信息有限→草稿+反问 / 信息极少→通用草稿+带选项反问）。
- 新增 `kind:"draft"` 输出类型：草稿正文含 [待补充:XXX] 标记，附带 questions 数组（2-4 选项 + 自定义）。
- 修复 Amazon 对话语言问题：原 Prompt 未区分"对话沟通"与"生成内容"，DeepSeek 对中文用户也用英文追问。
- Amazon 平台 Prompt 加入明确语言规则：对话沟通中文，Listing 正文英文。
- deepseek.py `_parse_content` 支持解析 `kind:"draft"`，与 `result` 共用内容结构。
- 测试更新：`test_prompt_forbids_inventing_product_facts` 适配新 Prompt 措辞；新增 `test_amazon_prompt_mandates_english_listing`。
- 验证：后端 8 个测试全部通过。
- 强化 draft 模式：Prompt 加入"信息不足必须返回 kind:draft 绝不能返回 kind:result"的硬规则。
- 新增 conversation_title：Prompt 要求模型每次返回 8-15 字会话标题，后端解析后前端自动更新侧栏标题（不再截取用户消息前 18 字）。
- 新增 questions 字段透传：后端 ChatResponse 增加 questions 列表，前端 ChatBubble 渲染为可点击选项按钮（QuestionChips），点击直接发送。
- [待补充:XXX] 样式优化：气泡和预览卡中的待补充标记渲染为黄色虚线高亮 badge，不再显示为纯文本。
- 验证：后端 8 测试通过；前端 build 通过。
- 模型切换：deepseek-v4-flash → deepseek-v4-pro（API 确认仅支持 v4-pro 和 v4-flash）。
- Prompt 重写：从通用"电商内容运营"升级为"10 年电商运营经验的内容专家"人设。新增内容质量标准（钩子开头、场景化卖点、自然转化、标签策略）、分平台详细指导（小红书真实感、抖音 3 秒留人、Amazon 搜索转化）。
- 前端新增「停止生成」按钮：InputBar pending 状态下显示红色停止按钮，AbortController 取消进行中的请求。
- 前端新增「重新生成」按钮：最后一条 AI 消息下方显示，点击删除该消息并重新发送上一条用户消息。
- 前端 api.ts 支持 AbortSignal 参数传入。
- 验证：后端 8 测试通过；前端 build 通过。
- PRD 自检：发现 4 个 V1 MVP 必须项缺失（闲聊、记忆、违禁词检测、抖音小店文案）。
- Prompt 增加闲聊能力：新增 kind:"chat" 类型，非商品消息正常对话，电商问题给专业建议。
- 新增违禁词检测模块（postprocess.py）：广告法通用违禁词 + 三平台特定违禁词 + 平台规则检查。
- ChatResponse 新增 warnings 字段，后处理结果透传给前端。
- Prompt 补充抖音小店文案内容类型。
- 验证：后端 13 测试通过（8 chat + 5 postprocess）。
- 前端 warnings 展示：ChatBubble 新增 WarningBanner 组件，违禁词警告以黄色提示条显示。
- 前端 pulse keyframes 清理：移除废弃的 pulse 动画，统一使用 typing-bounce。
- 记忆系统（SQLite）：新增 memory.py 模块，UserProfile 数据类（品牌名/品类/人群/调性/风格/平台/禁忌词/备注），CRUD 操作。
- 后端 /api/profile 端点：GET 获取、POST 保存、DELETE 删除。
- Prompt 注入记忆：DeepSeekClient 接受 profile 参数，build_memory_prompt 将品牌档案注入系统提示。
- 前端品牌档案面板：ProfilePanel 组件（模态框），支持编辑所有字段，顶栏 📋 按钮打开。
- 验证：后端 21 测试通过（8 chat + 8 memory + 5 postprocess）；前端 build 通过。

### 2026-06-08
- 完成 V3 UI 设计稿评审，选定 V3 方案
- 开始从前端搭建项目
- ⚠️ 前端视觉行为变更：原实现固定像素照搬设计稿，在 2K 屏尺度失衡；现改为统一 900px 内容轴、双栏生成结果卡和响应式移动布局。
- 补齐发送、Enter 快捷发送、真实剪贴板复制和移动端导航抽屉。
- 初始化 Git 仓库，当前分支：`feat/frontend-v3-polish`。
- 验证：`npm run lint` 通过；`npm run build` 通过。
- ⚠️ 产品范围变化：Amazon 从 V2 前置到 V1/M2；当前已完成前端入口，后端生成与平台规则尚未实现。
- Amazon 前端验证：`npm run lint`、`npm run build` 通过；浏览器点击 Amazon 后顶部标题正确切换为商品 Listing。
- 完成 `/api/chat` 首个真实生成闭环：FastAPI + DeepSeek V4 Flash + 前端异步调用。
- 后端增加输入/历史长度上限、30 秒超时、上游错误处理和 token 用量日志。
- 演示消息与真实 API 历史隔离，避免样例中的虚构信息污染真实生成。
- 完成真实会话管理：可创建新对话、独立保存消息、切换历史会话，并通过浏览器本地存储在刷新后恢复。
- 浏览器端到端验证：新建抖音会话 → 真实生成追问 → 侧栏切回演示会话，消息与平台均正确隔离。
- 移除输入框内的平台切换按钮；平台在创建会话时确定，避免聊天过程中误切平台污染上下文。
- 新对话改为平台预览选择页：直观展示小红书、抖音与 Amazon 的不同产出结构，选择后再创建对应平台会话。
- 修复新对话页返回问题：打开平台预览时保留当前会话，点击“对话”可正常返回；只有选定平台后才创建新会话。
- 修复平台预览卡样式未加载问题：重启并清理前端开发缓存，增加卡片宽度与按钮外观约束；验证计算样式恢复 Grid、边框与圆角。
- 对话生成结果升级为结构化应用卡片：追问继续显示普通气泡；成品按小红书、抖音、Amazon 分别显示平台预览并支持复制全文。
- 真实 DeepSeek 验证：信息不足返回普通追问；信息充分返回结构化 `result`。前端应用卡 CSS 已确认加载。
- 平台应用预览改为高保真终端视图：小红书手机帖子流、抖音竖屏视频与拍摄脚本、Amazon 商品详情页与 Offer 区。
- 浏览器验证小红书预览包含状态栏、导航、作者、首图位、正文标签和互动栏，宽度 360px、圆角 26px。
- ⚠️ 生成质量抽检发现模型会推断未提供的成分与体验事实，已加严系统 Prompt；少于 3 项明确产品事实时必须先追问。
- 验证：后端 6 个测试通过；真实 DeepSeek 抖音脚本调用成功；前端 lint/build 通过。

### 2026-06-09
- **商品图工作室三步流程全通**：生成三视图 → 调整外观 → 场景图生成，使用通义万相 Wan2.7-Image-Pro 异步模式。
- **OSS 超时修复**：URL 直传代替 base64 转码，避免服务器下载超时。Nginx `proxy_read_timeout` 提至 180s。
- **Studio 进侧栏**：生成三视图时自动创建 studio 类型会话，消息写入 conversation.messages，切换不丢状态。
- **侧栏优化**：最近活跃会话置顶、hydrate 时按 ID 去重、按 updated_at 排序。
- **工作台升级**：场景模板卡片加比例标签（`.studio-tpl-ratio`）、分类图标（`.studio-cat-icon`）、标签徽章（`.studio-tpl-tags`），自定义 prompt 区显示当前模板提示（`.studio-prompt-hint`）。
- **前端类型统一**：`useChat` 导出 `Conversation` 类型，Message 接口增加 `image` 字段，`appendMessage` 公开导出。
- **SOUL.md 硬规则前置**：方括号标签禁令移至文件顶部，附反面/正面示例。
- 验证：后端 62 测试通过，build 通过，三步 API 全通。部署路径修复（frontend/out/）。
- **场景生成移入工作台**：WorkspacePanel 新增「📸 场景」Tab，场景模板卡片 + 自定义 prompt + 生成/微调从 StudioView 移入工作台。StudioView 专注产品定义和三视图调整。
- **场景模板管理**：工作台「📸 场景」Tab 改为模板库管理（类似知识来源）。左侧模板列表，右侧查看 Prompt 内容。支持新建自定义模板（POST /api/studio/templates/custom）和删除（DELETE）。模板 API 增加 `prompt_template` 和 `builtin` 字段。自定义模板存储在 `backend/app/data/custom_scene_templates.json`。

### 2026-06-09 Studio 真实生图验收
- 使用真实 DashScope 配置完整调用 Studio 后端接口，未使用 mock：
  - 纯文字生成商品三视图成功，输出 2048×2048 PNG，耗时约 40 秒。
  - 基于三视图调整杯盖颜色成功，输出 2048×2048 PNG，耗时约 13 秒。
  - 基于调整结果生成办公桌场景图成功，输出 2048×2048 PNG，耗时约 13 秒。
- 视觉抽检：
  - 三视图整体干净，颜色和材质符合描述，但正面与侧面几乎相同，不满足稳定三视图要求。
  - 外观调整准确改变杯盖颜色，杯身和排版基本保持一致。
  - 场景图光线和氛围质量可用，但模型把三视图中的两个正面商品带入场景，生成了两个杯子。
- ⚠️ 前端链路实测失败：按项目文档分别启动 Next.js 与 FastAPI 后，`/shopgenie/api/studio/templates` 返回 404；后端 `/api/studio/*` 直连接口正常。
- ⚠️ 发现的实现问题：
  - 场景模板选择后，前端发送 `Template: {id}`，没有使用模板的 `prompt_template`。
  - Studio 状态未写入后端 session，刷新或从后端恢复后会丢失参考图、阶段和结果。
  - 首次进入 Studio 的异步生成流程可能因旧会话状态导致结果写不回或重复创建会话。
  - 前端允许上传 5MB 图片，但 base64 膨胀后可能超过后端 500 万字符限制。
  - 空描述且无图片时后端仍接受并提交生图任务，可能产生无效费用。
- 结论：**后端生图能力已跑通，前端完整链路和商品一致性尚未验收通过，Studio 暂不视为完成。**

### 2026-06-09 全量代码与前端审查
- 验证证据：
  - 后端测试：62 passed。
  - 前端测试：23 passed。
  - 前端生产 build：通过，但静态导出与 rewrites 配置产生警告。
  - 前端 lint：失败，1 个 error、15 个 warnings。
  - 浏览器抽检：桌面与 390×844 基础聊天布局可用，整体视觉已有产品感。
- ⚠️ 平台强契约没有按 PRD 执行：
  - 确定性结构校验失败后，目前只追加 warning，错误成品仍会展示并保存为内容资产。
  - 尚未实现“最多自动矫正一次，仍失败则不展示错误成品”。
  - 结果卡无条件显示“信息结构通过 / 平台格式已适配”，会与后端校验失败状态矛盾。
- ⚠️ 流式与非流式生成能力漂移：
  - 前端主要使用流式接口，但流式后处理没有传入品牌自定义禁忌词。
  - 流式最终响应固定返回空 `sources`，不会展示已检索到的参考来源。
- ⚠️ 平台与结果渲染契约不完整：
  - `Platform.STUDIO` 是合法后端枚举，但 `PLATFORM_PROMPTS` 没有对应条目；若进入聊天生成链路会触发 `KeyError`。
  - 客服话术 `Platform.CS` 可以生成结构化结果，但 `ResultCard` 没有客服结果预览分支，成品卡主体为空。
- ⚠️ 前端交互与可访问性：
  - 移动端进入 Studio 后没有顶栏、返回按钮或导航入口，用户可能被困在 Studio 页面。
  - 侧栏删除控件是会话按钮内部的可点击 `span`，且只在 hover 时显示，触屏和键盘用户难以操作。
  - 欢迎页 Studio 文案仍描述“智能抠图 → 换场景背景”，与当前三视图流程不一致。
- ⚠️ 测试覆盖不足：
  - 当前没有覆盖流式平台强契约失败、自动矫正、错误成品不展示、品牌禁忌词、参考来源的回归测试。
  - 当前没有覆盖 Studio API、Studio 状态恢复、客服结果卡和移动端 Studio 导航的自动化测试。

---

## 遗留与待办

- [x] ~~[P0][平台契约] 结构校验失败后不得展示或保存错误成品；实现最多一次自动矫正，仍失败则返回明确错误或待补充草稿。~~（2026-06-10：enforce_platform_contract + 测试）
- [x] ~~[P0][平台契约] 结果卡根据真实校验结果展示状态，禁止无条件显示“信息结构通过 / 平台格式已适配”。~~（2026-06-10：后端契约保证 + 矫正标识）
- [x] ~~[P0][流式链路] 流式生成接入品牌自定义禁忌词与参考来源，确保与非流式接口行为一致。~~（2026-06-10：真实 API 实测验证）
- [x] ~~[P0][Studio] 修复 Next.js 开发/部署 API 路由，使前端 Studio 能实际调用 `/api/studio/*`。~~（2026-06-10：rewrites source 去掉 basePath 前缀，curl 实测 200）
- [x] ~~[P0][Studio] 场景生成前只提取一个明确商品参考，避免把三视图误生成成多个商品。~~（2026-06-10：Prompt 明确单商品约束；实图质量待抽检）
- [x] ~~[P0][Studio] 修复首次生成时旧会话状态导致的结果写不回或重复创建会话问题。~~（2026-06-10：会话隔离 + 本地草稿修复）
- [x] ~~[P1][平台契约] 明确 `Platform.STUDIO` 不进入聊天生成链路，或补齐对应 Prompt，避免合法枚举触发 `KeyError`。~~（2026-06-10：两个 chat 端点 422 + 测试）
- [x] ~~[P1][前端] 优化客服话术结果预览卡，为每个客服场景/变体区块增加“复制此话术”及“微调”操作，完善交互样式。~~（2026-06-12）
- [x] ~~[P1][前端] 为移动端 Studio 增加返回或打开导航入口。~~（2026-06-10：topbar + 返回按钮，390x844 实测）
- [x] ~~[P1][前端] 将侧栏最近对话项重构为 div 容器内包含两个独立按钮的结构，支持 Tab 键聚焦与 Focus 状态删除按钮常显。~~（2026-06-12）
- [x] ~~[P1][前端] 修复当前 lint error~~（2026-06-10：0 error；15 个低优先级 warnings 待清理）
- [P1][部署] 统一静态导出、basePath、API 代理和部署路由策略，消除 build 的 rewrites 警告并验证生产路径。
- [x] ~~[P1][Studio] 场景模板实际使用 `prompt_template`~~（2026-06-10：已接通；前端"模板→请求内容"回归测试仍待补）
- [x] ~~[P1][Studio] 持久化 Studio 的阶段、参考图、调整历史和场景结果，验证刷新及切换会话不丢状态。~~（2026-06-10 续3：pendingTask 持久化 + 真实万相调用实测，提交→取消→刷新→恢复→结果落地）
- [x] ~~[P1][Studio] 统一前后端图片大小限制，按 base64 后实际长度校验并给出明确错误。~~（2026-06-10：InputBar/StudioView 与后端对齐 5M 字符）
- [x] ~~[P1][Studio] 拒绝空描述且无图片的生图请求。~~（2026-06-10：后端 422 + 测试）
- [x] ~~[P1][Studio] 统一任务轮询契约和总超时；上游失败或后端非 2xx 时前端立即停止。~~（2026-06-10：后端单次查询，前端控制循环与超时）
- [P1][Studio] 加强三视图 Prompt 和验收规则，确保正面、侧面、顶面视角明确不同。
- [P1][Studio] 为生成、调整、场景、失败轮询、模板与状态恢复补齐后端和前端测试。
- [x] ~~[P2][前端] 更新欢迎页 Studio 文案，使其与“三视图生成 → 调整外观 → 场景图生成”实际流程一致。~~（2026-06-12）
- 内测用户来源待确认（盛哥渠道？小红书？）
- 抖音生成 Prompt 仍需更多真实商品样本做质量抽检。
- [P1][账号] 品牌档案和工作台数据仍是后端单用户 `default` 数据；前端虽发送 `X-User-Id`，后端尚未验证或使用该请求头。正式多用户使用前需要真实登录、鉴权与数据隔离。
- [x] ~~公开网页发现结果仍需商家判断来源可信度，暂未针对每个平台建立官方域名优先级。~~（2026-06-12：已在 `web_search.py` 实现基于官方/高可信度/媒体域名库的可信度分级评估，自动在前台检索列表的 title 前添加可信度前缀）
- [x] ~~[P3][数据回流] 平台 API 数据连接器~~（2026-06-15：配置驱动只读首版；支持连接状态与显式同步，远端记录全量校验、幂等原子写入；实际平台授权与接口适配由部署配置提供）
- [x] ~~服务器 git 仓库代码通过 scp 传输，git 历史落后于 GitHub，需要同步。~~（2026-06-10：服务器改为 git over SSH 只读部署密钥，已同步到 `origin/main`；远程地址不再内嵌 token）
- ⚠️[安全] 旧的 `gho_` GitHub token 曾明文写在服务器远程地址里、已暴露，需用户在 github.com/settings/applications 撤销（服务器已改用 SSH 部署密钥，撤销无副作用）。
- ⚠️[运维] 服务器后端是手动 `setsid uvicorn` 进程（非 supervisor 托管），重启服务器不会自动拉起；建议挂入 pm2 / systemd 做开机自启 + 崩溃自拉。
- 服务器后端为手动重启部署，无 CI/CD；前端为静态导出（`frontend/out`），改动需 `npm run build` 后由 nginx 提供，`/api` 由 nginx 反代至 8000（Next rewrites 在 export 下不生效，属预期）。

---

*规则：完成一项立即更新，做不完如实标注 ⚠️*

---

## 2026-06-12 定版（v1 Final · 转入求职作品状态）

- ✅ 回填提醒：排期发布 ≥3 天且无效果数据的资产，指挥台自动生成回填建议（含测试）。
- ✅ useChat 持久化只写脏会话（对象引用比对），消除 O(N) 全量回写。
- ✅ 首页定版精修：衬线 masthead 问候 + 状态行；健康态/建议不足 3 条时"最近内容"补位；资产概览轻量化为列表行；快捷创作入口压成一行；"需要关注"badge 归位到标题旁；修复 cockpit grid 子项 min-width:auto 导致的列宽塌陷。
- ✅ 效果数据 KPI 空状态改为引导文案（不再显示 0% 假指标）。
- ✅ 字阶清理：32 处中文界面文字提升至 ≥10.5/11px；平台 mock 仿真区与装饰性英文标签维持原样。
- ✅ `backend/scripts/seed_demo.py`：一条命令灌入演示数据（3 商品+评论洞察 / 5 资产 / 趋势效果数据 / 双 A/B），指挥台五类规则全部点亮；默认拒绝覆盖已有数据，`--force` 显式清空，notes 标注 demo 来源。
- ✅ README 重写为求职门面：定位、闭环 mermaid 图、设计决策、工程质量、快速开始。
- ✅ 回归：后端 111 passed / 前端 31 passed / lint 0 error / build 通过 / 浏览器实测一屏布局 + 无 console 错误。
- 📌 明确不做（定版边界）：路由化一级页面、WorkspacePanel 拆分、消息 ID 重构、截图识别录入、周报——记录于 PRD 4.7/4.8，留待后续批次。

## 2026-06-15（抖音商品文案与平台契约修复）

- 新增 `content_type` 内容类型契约，抖音短视频脚本与抖音小店商品文案使用独立生成规则、确定性校验和结果预览；旧内容默认兼容。
- 抖音脚本收紧为至少 3 个完整分镜，每个分镜必须包含时间段、画面/镜头说明和口播，并检查开头 Hook 与结尾转化动作。
- 小红书确定性拒绝分秒脚本污染；Amazon 收紧为 3-5 条英文 Bullet Points。
- 当前会话明确请求生成另一平台内容时，后端在调用模型前要求选择“转换为当前平台”或“新建目标平台会话”。
- 修正台账中的错误完成记录：后端尚未实现租户隔离与 `/api/performance/sync`；PRD 平台规则来源同步为 `prompts.py`。
- 验证：后端 `120 passed`；前端 `32 passed`；生产 build 通过；lint `0 errors`；本地浏览器基础页面无前端控制台错误。

## 2026-06-15（平台 API 只读连接器 + 一级路由化）

- 新增配置驱动的平台 API 只读连接器：状态查询与显式同步，支持小红书、抖音、Amazon；前端不能提交任意 URL 或令牌。
- 平台数据按稳定 `record_id` 幂等入库；同步前校验资产、平台归属、非负数与漏斗关系，任一错误整批拒绝。
- 工作台全部模块升级为一级路由：`/products`、`/content`、`/experiments`、`/knowledge`、`/tasks`、`/performance`、`/calendar`；查询参数保留商品与内容深链。
- 静态导出启用 trailing slash，每个一级页面均生成独立 HTML 目录，适配 nginx 静态部署。
- 浏览器走查发现并修复实验 SWR fetcher 把缓存 key 错当 `product_id` 的旧问题。
- 验证：后端 `124 passed`；前端 `36 passed`；lint `0 errors`；生产 build 生成 8 个业务静态页面；浏览器验证 `/performance/` 直达、连接器面板与 `/products/` 路由切换无控制台错误。

## 2026-06-15（一级路由前端展示与体验验收）

- 修复一级路由页沿用弹窗滚动策略导致长内容超出视口后无法继续浏览的问题；页面固定在视口内，由工作区正文独立滚动。
- 一级路由页标题与说明改为当前模块内容，不再统一显示“内容工作台”。
- 横向模块导航增加当前页语义与自动定位；移动端直达靠后的“效果数据 / 营销日历”时，当前标签会自动滚到可见区域。
- 工作台改为按当前模块请求必要数据，避免打开任意一级页时并发加载全部七个模块。
- 收紧移动端工作台标题、导航、正文和卡片间距，提升 390px 宽度下的信息密度。
- 验证：前端 `39 passed`；lint `0 errors`（12 个既有 warnings）；生产 build 通过；浏览器逐页验证 7 个一级路由在 390×844 下无横向溢出、标题与当前标签一致、长内容可滚动、返回对话与移动侧栏正常、控制台无错误。
